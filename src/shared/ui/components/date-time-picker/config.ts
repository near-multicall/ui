export class DateTimePickerConfig {
    static readonly #rootClass = "DateTimePicker";

    static readonly classes = {
        root: DateTimePickerConfig.#rootClass,
        modal: DateTimePickerConfig.#rootClass + "-modal",
    };
}
